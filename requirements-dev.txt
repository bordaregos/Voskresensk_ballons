# Зависимости для тестирования бизнес-логики (src/services).
# Намеренно не зависит от requirements.txt (без PyQt6/docxtpl) — это
# доказывает, что расчётный слой не завязан на Qt.

pytest>=7.4
pytest-cov>=4.1
python-docx>=1.1.0  # template_generator.py/template_validator.py — не тянет PyQt6/docxtpl/Jinja2
